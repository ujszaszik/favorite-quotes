<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.newstore.favqs">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk tools:overrideLibrary="androidx.security" />

    <application
        android:name=".application.QuotesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_favqs"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_favqs_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FavQs">
        <activity
            android:name="com.newstore.favqs.features.main.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>